# Background Remover

A computer vision project built for practice.

Draw a rectangle around any subject in a photo and the app automatically segments it from the background using the GrabCut algorithm. Refine the result by painting green (keep) or red (remove) strokes. Export the result as a transparent PNG.

## Run
pip install opencv-python numpy Pillow
python co3_background_remover.py
